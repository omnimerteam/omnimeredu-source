import 'package:flutter/material.dart';

class CreateAttendanceScreen extends StatelessWidget {
  const CreateAttendanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Attendance Sheet')),
      body: const Center(child: Text('Create Attendance Screen Placeholder')),
    );
  }
}
