services:
  app:
    container_name: ${CONTAINER_NAME}-container

    image: ${IMAGE_NAME}:latest

    ports:
      - "${HOST_PORT}:${PORT}"   # Map host:container
    
    environment:
      - PORT=${PORT}             # Truyền env cho Node.js
    
    env_file:
      - .env                     # Tự load file .env ngoài host, không cần trong image
      - omnimeredu-bb808-firebase-adminsdk-fbsvc-59682697af.json
    restart: always
    # volumes:
    #   - .:/usr/src/app           # Optional: để dev có hot reload (không dùng cho production)
